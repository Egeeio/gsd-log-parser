version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run:
          name: Install npm deps
          command: "npm install"
      - run:
          name: Build Project
          command: "npm run build"
